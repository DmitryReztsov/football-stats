
export interface ITheme {
  colors: {
    dark_green: string,
    middle_green: string,
    light_green: string,
    light_brown: string,
    light_gray: string,
    white: string,
    black: string,
  }

  media: {
    extraLarge: string
    large: string
    medium: string
    small: string
  }
}