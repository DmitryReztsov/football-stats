export interface ILightTheme {
  colors: {
    dark_green: string,
    middle_green: string,
    light_green: string,
    light_brown: string,
    bg: string,
    font_light: string,
    font_dark: string,
  }

  media: {
    extraLarge: string
    large: string
    medium: string
    small: string
  }
}